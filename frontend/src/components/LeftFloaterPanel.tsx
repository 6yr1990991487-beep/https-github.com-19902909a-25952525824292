import React from "react";

// Minimal stub for LeftFloaterPanel to satisfy build when the real component
// is missing in some deployment contexts. Replace with the full implementation
// if/when available.
export default function LeftFloaterPanel() {
  return (
    <div aria-hidden="true" className="left-floater-placeholder" />
  );
}
